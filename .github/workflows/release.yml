# Validate a version tag, build its artifacts, approve, and publish to PyPI.
name: Release

on:
  push:
    tags:
      - "v*"

permissions: {}

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    name: Validate release version
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Match tag, package version, and PyPI
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          python - <<'PY'
          import os
          import re
          import tomllib
          import urllib.error
          import urllib.request
          from pathlib import Path

          version = tomllib.loads(Path("pyproject.toml").read_text())["project"]["version"]
          tag = os.environ["RELEASE_TAG"]
          if tag != f"v{version}":
              raise SystemExit(f"tag {tag!r} does not match project version {version!r}")
          if not re.fullmatch(r"\d+\.\d+\.\d+", version):
              raise SystemExit(f"release version must be a plain three-part version: {version!r}")

          url = f"https://pypi.org/pypi/mintalib/{version}/json"
          try:
              urllib.request.urlopen(url, timeout=10)
          except urllib.error.HTTPError as error:
              if error.code == 404:
                  print(f"Validated unpublished release {version}")
              else:
                  raise
          else:
              raise SystemExit(f"mintalib {version} already exists on PyPI")
          PY

  build:
    name: Build and verify
    needs: validate
    permissions:
      contents: read
    uses: ./.github/workflows/build.yml

  publish:
    name: Confirm and publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/mintalib
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: dist-*
          merge-multiple: true
          path: dist

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0

      - name: Publish release artifacts
        run: uv publish --trusted-publishing always dist/*
